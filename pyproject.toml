[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "django",
    "google-api-python-client",
    "google-auth-httplib2",
    "google-auth-oauthlib"
]
build-backend = "setuptools.build_meta"